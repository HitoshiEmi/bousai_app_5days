# 避難所検索結果画面を再現するための実装プロンプト

あなたは既存の Web アプリケーションを調査・編集する熟練した開発者です。以下の要件をすべて満たすように、避難所検索結果画面を実装してください。

## 目的

添付の画面イメージに忠実な「避難所検索結果」画面を作成してください。

実装対象は避難所検索結果画面のみです。既存のトップページ、ログイン画面、避難所登録画面、指示ボードなど、関係しない画面のデザインや機能は変更しないでください。

## 作業前に行うこと

1. リポジトリ内の構成を確認する。
2. Flask など既存のフレームワーク、テンプレート継承、ルーティング、データ形式を確認する。
3. 既存の検索結果画面テンプレートと、その画面を表示するルートを特定する。
4. 既存の機能やデータを可能な限り流用し、不要な大規模リファクタリングは行わない。
5. 編集前に、実装対象と変更ファイルを明確にする。

## 画面全体のレイアウト

画面イメージを基準に、以下の構成にしてください。

- 白背景
- 画面中央に、黒または濃いグレーの太い枠線で囲まれた大きなコンテナを配置する
- デスクトップでは画面幅を広く使い、画像のワイヤーフレームに近い固定感のあるレイアウトにする
- 大きなコンテナの左上に、グレーの背景色を持つタイトル領域を配置する
- タイトルは「避難所検索結果」とする
- タイトルは中央寄せで、過度に太くしない
- コンテナ右上に、現在日時と「更新」ボタンを配置する
- 現在日時は日本語で、次のような形式にする
  - `2026年09月10日 12時30分現在`
- 「更新」ボタンを押すと画面を再読み込みする
- コンテナ左側に縦方向のナビゲーションを配置する
  - 「ホーム」
  - 「戻る」
- 「ホーム」はトップページへのリンクにする
- 「戻る」は避難所検索画面へのリンクにする
- ボタンや枠線は、画面イメージに合わせて淡い水色、薄いグレー、濃いグレーを使用する
- 角丸は控えめにし、カード型の現代的なデザインへ大きく変更しない

## メイン領域

コンテナ内のメイン領域は、次の3列構成にしてください。

1. 左列: 地図
2. 中央列: 避難所情報一覧
3. 右列: 道路状況一覧

### 地図

- 地図領域は枠線で囲む
- Leaflet が既に導入されている場合は Leaflet を使用する
- 外部地図ライブラリがない場合は、既存の依存関係を確認して必要最小限の方法で導入する
- 地図の初期表示地点は青森市にする
- 青森市中心部の座標として、次を使用する
  - 緯度: `40.8222`
  - 経度: `140.7474`
- 初期ズームは市街地を確認できる程度にする。目安は `12`
- `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png` の OpenStreetMap タイルを使用してよい
- OpenStreetMap の attribution を表示する
- 表示対象の避難所ごとに地図上へマーカーを表示する
- 避難所に緯度経度が実データとして存在しない場合は、青森市中心部付近に仮の座標を割り当ててよい
- マーカーをクリックすると避難所名を表示する

### 避難所情報一覧

- 中央列の上部に「避難所情報一覧」と表示する
- 避難所一覧の直上に、属性選択用のプルダウンを配置する
- プルダウンの初期表示は「▼ 選択してください」とする
- 地区選択用の「青森市」「藤沢市」のプルダウンは作成しない
- 今回の検索画面・登録画面は未完成のため、避難所属性は仮データでよい
- 以下の属性を用意する
  - `バリアフリー対応`
  - `ペット同行可`
  - `医療救護対応`
- 既存の避難所データに属性がない場合は、テンプレート側または表示用データ側で仮属性を割り当てる
- 各避難所行に、避難所名と属性名を表示する
- プルダウンで属性を選択したとき、該当属性の避難所だけを表示する
- 「▼ 選択してください」を選んだ場合は、すべての避難所を表示する
- 絞り込みはページ全体を再読み込みせず、JavaScript で即時反映してよい
- 避難所がない場合は「該当する避難所が見つかりませんでした」と表示する
- 避難所がない場合は、画像イメージに近い赤系のエラーメッセージ枠も表示する

### 道路状況一覧

- 右列に「道路状況一覧」を表示する
- 枠線で囲まれた一覧領域にする
- 道路状況の実データがまだない場合は、次の仮文言を表示する
  - `現在、情報はありません`

## 見た目の目安

次の値を目安に、添付画像に近い比率で調整してください。画面幅に応じて破綻しないようにしてください。

- 大枠の最大幅: 約 `1425px`
- 大枠の最小高さ: 約 `825px`
- 大枠上部の余白: 約 `128px`
- タイトル領域: 幅 約 `632px`、高さ 約 `130px`
- 左ナビゲーションの幅: 約 `147px`
- 地図・一覧の高さ: 約 `579px`
- 列間の間隔: 約 `30px`
- タイトル背景: `#d8d8d8` 程度
- ボタン背景: `#dcecf8` 程度
- 全体の文字色: `#303030` 程度
- 外枠: `#333` 程度
- 内側の枠線: `#aaa` 程度

完全なピクセル固定ではなく、デスクトップで添付画像に近く見えることを優先してください。

## レスポンシブ対応

- デスクトップでは、地図・避難所一覧・道路状況一覧の3列を維持する
- 画面幅が狭くなった場合は、2列または縦積みに切り替える
- スマートフォンで横スクロールが発生しないようにする
- タイトル、日時、ボタン、プルダウン、一覧内の文字が重ならないようにする
- モバイルでは各パネルの高さを適切に縮める
- 既存の共通ヘッダーがある場合、この結果画面だけは画像に合わせて非表示にしてよい

## 技術上の条件

- 既存のテンプレート継承やルーティング方式を維持する
- 既存の `results` などの検索結果データを利用する
- エスケープ処理を維持し、避難所名を安全に表示する
- JavaScript は対象画面に必要な範囲に限定する
- 外部 CDN を使う場合は、既存プロジェクトの方針に合わせる
- 不要なファイル、不要な依存関係、不要な API は追加しない
- 既存の別画面の表示を壊さない
- コードは既存プロジェクトの記法・インデントに合わせる

## 実装後の確認

実装後、必ず次を確認してください。

1. 検索結果画面の URL が正常に表示される
2. HTTP ステータスが `200` になる
3. タイトル「避難所検索結果」が表示される
4. 青森市中心部の地図が表示される
5. 地図に避難所マーカーが表示される
6. 避難所名と仮属性が一覧に表示される
7. 属性プルダウンが避難所一覧の直上にある
8. 属性を選ぶと該当する避難所だけに絞り込まれる
9. 「▼ 選択してください」に戻すと全件表示される
10. 「ホーム」「戻る」「更新」が機能する
11. 避難所が空の場合もエラー表示が崩れない
12. スマートフォン幅でもレイアウトが崩れない
13. テンプレート構文エラー、Python エラー、JavaScript エラーがない
14. `git diff --check` などで不要な空白エラーがない

## 完了条件

上記要件を満たすまで実装・確認を行ってください。問題がある場合は、原因を特定して修正し、同じ確認を再実行してください。最後に、変更したファイル、実装した機能、実行した確認内容を簡潔に報告してください。
